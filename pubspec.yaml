name: dotenv_gh_pages
description: Test .env in GitHub Pages

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  envify: any
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  envify_generator: any
  build_runner: any

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true